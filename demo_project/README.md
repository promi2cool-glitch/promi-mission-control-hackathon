# demo_project

A small, sanitized, self-contained project that a dispatched Claude Code worker will actually operate on during a demo mission. Contains no Promi source code — it exists solely so the mission/verification loop has real files/tools/tests to act on (PLAN.md phase 3).

Not populated yet — see `PLAN.md` for phase order.
