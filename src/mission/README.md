# src/mission

Structured mission and result contracts: what Promi sends to the XO adapter to dispatch a mission (PLAN.md phase 2), and the structured result shape the worker's run is translated into (phase 6).

Not implemented yet — see `PLAN.md` for phase order.
