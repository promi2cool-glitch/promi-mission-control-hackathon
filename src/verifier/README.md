# src/verifier

Independently checks a structured mission result against the mission's stated success criteria and emits PASS / FAIL / PARTIAL / BLOCKED (PLAN.md phase 7). Worker completion is not treated as a verification pass — see AGENTS.md §12.

Not implemented yet — see `PLAN.md` for phase order.
